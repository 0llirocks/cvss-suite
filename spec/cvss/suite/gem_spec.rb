require 'spec_helper'

describe Cvss::Suite::Gem do
  it 'has a version number' do
    expect(Cvss::Suite::Gem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
