name: RSpec

on: [push,pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Ruby
      uses: actions/setup-ruby@v1
      with:
        ruby-version: [ '2.0.x', '2.6.x' ]
    - name: Build
      run: |
        gem install bundler -v "~> 1.10"
        bundle install --jobs 4 --retry 3
    - name: Run tests
      run: bundle exec rspec spec
